import { StyleSheet } from 'react-native';
import { common } from '../../../../styles/common';

export default StyleSheet.create({
    container: {
        flex: 1,
        paddingHorizontal: 16
    },
    buttonContainer: {
        marginTop: 20,
        paddingBottom: 32
    },
    empty: {
        paddingTop: 32
    },
    emptyTextContainer: {
        marginTop: 32
    },
    emptyText: common.text
});
