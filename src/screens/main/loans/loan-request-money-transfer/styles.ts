import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    addButton: {
        marginTop: 20
    },
    btnContainer: {
        marginTop: 8,
        marginBottom: 24,
        marginHorizontal: 8
    }
});
