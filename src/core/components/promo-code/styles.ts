import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    applyText: { color: '#172A56' },
    infoTextApply: { color: '#00B894' },
    errorText: { color: '#E13A3A' },
    inputContainer: { marginBottom: 16 }
});
